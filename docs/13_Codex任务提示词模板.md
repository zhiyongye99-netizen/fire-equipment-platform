# 13_Codex任务提示词模板

## 首次进入 Codex

请先阅读 AGENTS.md 和 docs/ 下所有文档，不要写代码。

请输出你对以下内容的理解：项目目标、产品边界、技术栈、三端结构、供应商轻工作台、用户角色与权限、装备分类和参数模板、第一阶段开发顺序、需求缺口和风险点。

本轮不要创建文件，不要安装依赖，不要修改代码。

## 创建项目骨架

请根据 AGENTS.md 和 docs/，创建项目基础骨架。

只创建 monorepo、Taro 小程序、Next.js 供应商后台、Next.js 平台后台、NestJS 后端、Prisma、ESLint、Prettier、TypeScript、pnpm workspace、基础路由和空页面。

不要实现业务功能，不要写死 mock 数据，不要修改 docs/ 和 AGENTS.md。

完成后运行 pnpm lint、pnpm typecheck、pnpm test。

## 单模块开发模板

任务名称：

任务背景：

请先阅读：

实现范围：

不要做：

验收标准：

完成后输出：修改文件、实现功能、测试结果、仍需人工确认事项。
