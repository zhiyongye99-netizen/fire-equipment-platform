 "use client";

import { Card, Typography } from "antd";

export default function DashboardPage() {
  return (
    <main style={{ padding: 24 }}>
      <Card>
        <Typography.Title level={1}>控制台</Typography.Title>
      </Card>
    </main>
  );
}
