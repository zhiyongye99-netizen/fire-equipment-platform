 "use client";

import { Card, Typography } from "antd";

export default function ParameterTemplatesPage() {
  return (
    <main style={{ padding: 24 }}>
      <Card>
        <Typography.Title level={1}>参数模板</Typography.Title>
      </Card>
    </main>
  );
}
